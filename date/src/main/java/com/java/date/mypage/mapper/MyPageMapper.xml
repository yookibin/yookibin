<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.myPageMapper">
	<delete id="delete" parameterType="Integer">
		delete save_coursee where num=#{num}
	</delete>
	
	<select id="getMyCourse" parameterType="String" resultType="com.java.date.recommandPlace.dto.SaveCourseDto">
		select * from save_coursee where save_cid=#{id}
	</select>
</mapper>








